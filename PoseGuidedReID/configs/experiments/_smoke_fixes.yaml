_base_: 01_fixes_only.yaml

DATA:
  SUBSAMPLE_PATH: configs/data/subsample_6ids_smoke.json

SOLVER:
  IMS_PER_BATCH: 16
  MAX_EPOCHS: 1
  WARMUP_EPOCHS: 0

DATALOADER:
  NUM_INSTANCE: 4
  NUM_WORKERS: 0

EVAL:
  VAL_EVERY_N_EPOCHS: 1
